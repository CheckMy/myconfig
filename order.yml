env: "config-server test3 !"
# 前缀配置
girl:
  name: "张三12"
  age: 12
# 这里的 order 文件想当与 application.yml
server:
  port: 8081

PRODUCT:
  ribbon:
    NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule

logging:
  level:
    com.cloud.product.mapper: debug
